# Core dependencies for MCP server
mcp>=1.0.0

# ASGI server (runs the MCP streamable HTTP app)
uvicorn>=0.20.0

# Environment handling
python-dotenv>=1.0.0

# HTTP client (fetcher downloads)
httpx>=0.24.0

# HTML/XML parsing for deterministic law source parsers
beautifulsoup4>=4.12.0
lxml>=5.0.0
